% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reproduce-simulations.R
\name{reproduce_hdzacp_analytic_size}
\alias{reproduce_hdzacp_analytic_size}
\title{Reproduce the direct asymptotic-calibration appendix}
\usage{
reproduce_hdzacp_analytic_size(
  output_dir,
  ...,
  pairs = data.frame(n = c(100L, 200L, 200L, 500L, 500L, 1000L), p = c(100L, 200L, 500L,
    200L, 500L, 500L)),
  designs = c("independent", "block", "ar1"),
  rho = 0.3,
  block_size = 3L,
  repetitions = 2000L,
  alphas = c(0.01, 0.05, 0.1),
  eta = 0.1,
  ou_paths = 200000L,
  ou_grid = 32768L,
  cores = 16L,
  seed = 20260909L,
  quick = FALSE
)
}
\arguments{
\item{output_dir}{Output directory.}

\item{...}{Reserved for forward compatibility; currently must be empty.}

\item{pairs}{Two-column data frame or matrix of \verb{(n,p)} pairs.}

\item{designs}{Any subset of \code{"independent"}, \code{"block"}, and \code{"ar1"}.}

\item{rho}{Correlation for block and AR(1) designs.}

\item{block_size}{Block size.}

\item{repetitions}{Monte Carlo replications per configuration.}

\item{alphas}{Nominal levels summarized from common p-values.}

\item{eta}{Trimming fraction.}

\item{ou_paths, ou_grid}{OU reference resolution.}

\item{cores}{Number of outer workers.}

\item{seed}{Reproducibility seed.}

\item{quick}{Downscale replications and OU resolution without dropping cells.}
}
\value{
A list containing raw p-values, size summaries, MAX quantiles,
calibration objects, and output paths.
}
\description{
The full preset evaluates six \verb{(n,p)} pairs under independent coordinates,
independent blocks of size three with correlation \code{0.3}, and AR(1)
correlation \code{0.3}.  It uses 2,000 null samples per configuration and an
independent 200,000-path, 32,768-step OU reference distribution.  No
permutation or empirical critical-value fitting is used.
}
