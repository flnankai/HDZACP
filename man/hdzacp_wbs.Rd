% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wbs.R
\name{hdzacp_wbs}
\alias{hdzacp_wbs}
\alias{zac_wbs}
\alias{zam_wbs}
\alias{zas_wbs}
\title{Wild binary segmentation with ZAC, ZAM, and ZAS}
\usage{
hdzacp_wbs(
  x,
  methods = c("ZAC", "ZAM", "ZAS"),
  calibration = c("permutation", "asymptotic", "analytic"),
  eta = 0.1,
  alpha = 0.05,
  n_intervals = 50L,
  min_interval = 40L,
  include_full = TRUE,
  intervals = NULL,
  permutations = 99L,
  stopping = c("bonferroni", "paper", "unadjusted"),
  seed = 20260824L,
  ties = c("error", "random"),
  tie_seed = seed,
  ou_reference = NULL,
  asymptotic_control = hdzacp_asymptotic_control(),
  keep_evidence = TRUE
)

zac_wbs(x, ...)

zam_wbs(x, ...)

zas_wbs(x, ...)
}
\arguments{
\item{x}{Numeric observation-by-coordinate matrix.}

\item{methods}{Any subset of ZAC, ZAM, and ZAS.}

\item{calibration}{Calibration passed to \code{\link[=hdzacp_test]{hdzacp_test()}}.}

\item{eta, alpha}{Boundary fraction and level.}

\item{n_intervals}{Number of random intervals, excluding the optional full
interval.}

\item{min_interval}{Minimum interval length.}

\item{include_full}{Include \verb{[0,n]}.}

\item{intervals}{Optional user-supplied split-after interval matrix. When
supplied, random interval controls are ignored.}

\item{permutations}{Local permutation count.}

\item{stopping}{One of \code{"bonferroni"}, \code{"paper"}, or \code{"unadjusted"}.}

\item{seed}{Seed for interval generation and local tests.}

\item{ties, tie_seed}{Tie controls passed to \code{\link[=hdzacp_test]{hdzacp_test()}}.}

\item{ou_reference, asymptotic_control}{Asymptotic controls.}

\item{keep_evidence}{Retain the interval-level table.}

\item{...}{Additional arguments passed by the ZAC, ZAM, and ZAS convenience
wrappers to \code{hdzacp_wbs()}.}
}
\value{
An object of class \code{hdzacp_wbs}.
}
\description{
Each random interval is sampled uniformly from all eligible intervals, and
the full interval can be added separately. A change point is the index of the
final observation before a distributional change. \code{stopping = "unadjusted"}
reproduces the article's simulation and empirical WBS convention. The safer
general default, \code{"bonferroni"}, adjusts by the number of tested intervals.
\code{"paper"} uses the article's analytic interval adjustment and truncated
component p-values for asymptotic ZAC.
}
