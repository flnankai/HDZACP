% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core.R
\name{hdzacp_test}
\alias{hdzacp_test}
\alias{zac_test}
\alias{zam_test}
\alias{zas_test}
\title{Test for a high-dimensional distributional change point}
\usage{
hdzacp_test(
  x,
  methods = c("ZAC", "ZAM", "ZAS"),
  calibration = c("permutation", "asymptotic", "analytic"),
  eta = 0.1,
  alpha = 0.05,
  permutations = 999L,
  seed = 20260821L,
  cauchy_weight = 0.5,
  ties = c("error", "random"),
  tie_seed = seed,
  ou_reference = NULL,
  asymptotic_control = hdzacp_asymptotic_control(),
  keep_permutations = FALSE
)

zac_test(x, ...)

zam_test(x, ...)

zas_test(x, ...)
}
\arguments{
\item{x}{Numeric data matrix with observations in rows.}

\item{methods}{Any subset of \code{"ZAC"}, \code{"ZAM"}, and \code{"ZAS"}.}

\item{calibration}{\code{"permutation"} (recommended), \code{"asymptotic"}, or its
alias \code{"analytic"}.}

\item{eta}{Boundary fraction.}

\item{alpha}{Test level.}

\item{permutations}{Number of random permutations; the orbit has one
additional observed ordering.}

\item{seed}{Integer seed passed explicitly to the C++ permutation generator.}

\item{cauchy_weight}{ZAS weight for asymptotic ZAC. Permutation ZAC follows
the article's equal-weight symmetric orbit and therefore requires \code{0.5}.}

\item{ties}{Tie handling. Random tie breaking is permitted only with
permutation calibration.}

\item{tie_seed}{Seed for random tie breaking.}

\item{ou_reference}{Optional object from \code{\link[=hdzacp_ou_reference]{hdzacp_ou_reference()}} or a numeric
vector of fine-grid maxima.}

\item{asymptotic_control}{Control from \code{\link[=hdzacp_asymptotic_control]{hdzacp_asymptotic_control()}}.}

\item{keep_permutations}{Retain component and Cauchy orbit statistics.}

\item{...}{Additional arguments passed by the ZAC, ZAM, and ZAS convenience
wrappers to \code{hdzacp_test()}.}
}
\value{
An object of class \code{hdzacp_test}; \verb{$results} is a tidy method table.
}
\description{
\code{ZAS} is the dense sum aggregation, \code{ZAM} is the sparse maximum aggregation,
and \code{ZAC} adaptively combines their evidence. The default uses a shared
whole-vector permutation orbit. Consequently each permutation keeps all
coordinates of an observation together and preserves cross-sectional
dependence. ZAC is ranked symmetrically within that same orbit; it is not
obtained by simply applying a Cauchy CDF to two observed permutation p-values.
}
