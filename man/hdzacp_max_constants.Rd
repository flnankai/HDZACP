% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analytic.R
\name{hdzacp_max_constants}
\alias{hdzacp_max_constants}
\title{Deterministic asymptotic constants for ZAM}
\usage{
hdzacp_max_constants(
  n,
  p,
  eta = 0.1,
  cache_dir = file.path(tools::R_user_dir("HDZACP", "cache"), "max-calibration"),
  t_nodes = 17L,
  endpoint_M = 1024L,
  moment_M = 4096L,
  ode_tol = 1e-10,
  verbose = interactive()
)
}
\arguments{
\item{n, p}{Sample size and dimension.}

\item{eta}{Boundary fraction.}

\item{cache_dir}{Directory for deterministic intermediate calculations.}

\item{t_nodes}{Number of integration nodes on the half interval.}

\item{endpoint_M}{Endpoint-recursion truncation.}

\item{moment_M}{Moment-series truncation.}

\item{ode_tol}{ODE tolerance.}

\item{verbose}{Emit numerical progress messages.}
}
\value{
A list containing scale \code{a}, center \code{b}, branch information, numerical
diagnostics, and the constants grid.
}
\description{
Computes the current manuscript's deterministic subcritical normalization
for the ZAM maximum scan. The calculation uses the Hardy ODE, Bellman
terminal weights, the exact Bernoulli endpoint recursion implemented in C++,
and deterministic quadrature. It never fits a Gumbel law to simulated null
statistics.
}
\details{
The present numerical implementation deliberately stops for critical or
boundary regimes instead of substituting a surrogate formula. Permutation
calibration is consequently the package default and is also preferable when
the asymptotic weak-dependence conditions are doubtful.
}
